name: test
on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      RELEASE_VERSION: ${{steps.set_output.outputs.RELEASE_VERSION}}
    steps:
      - name: Set the release version manually
        id: set_version
        run: echo "RELEASE_VERSION=1.0.1" >> "$GITHUB_OUTPUT"