name: test
on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set the release version manually
        id: "set_version"
        run: echo "RELEASE_VERSION=1.0.1" >> $GITHUB_ENV
  test2:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: ${{RELEASE_VERSION}}